describe('AdminDashboard Component Tests', () => {
    it('should return true', () => {
      expect(true).toBe(true);
    });
  
    it('should not return false', () => {
      expect(false).toBe(false); // This will fail to demonstrate passing and failing cases
    });
  
    it('should check a simple condition', () => {
      const result = 2 + 2; // Simple arithmetic check
      expect(result).toBe(4); // This will pass
    });
  });
  